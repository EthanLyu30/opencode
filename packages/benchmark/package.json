{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@opencode-ai/benchmark",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "task24": "bun src/cli.ts",
    "migrate:generate": "bun script/generate-run-migrations.ts",
    "browser:manifest": "bun script/build-browser-manifest.ts",
    "test": "bun test --timeout 30000 --only-failures",
    "typecheck": "tsgo --noEmit"
  },
  "dependencies": {
    "axe-core": "catalog:",
    "effect": "catalog:",
    "playwright": "catalog:",
    "sharp": "catalog:"
  },
  "devDependencies": {
    "@tsconfig/bun": "catalog:",
    "@types/bun": "catalog:",
    "@typescript/native-preview": "catalog:"
  }
}
